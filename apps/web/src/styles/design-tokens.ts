/**
 * Raw design token values for contexts where CSS variables are unavailable
 * (e.g. global-error.tsx). Keep in sync with forge-tokens.css.
 */
export const forgeTokens = {
  bg: '#121214',
  text: '#fafafa',
  textMuted: '#a1a1aa',
  primary: '#8b5cf6',
  primaryHover: '#a78bfa',
  radiusMd: '10px',
} as const;
